import { Express } from 'jest-express/lib/express';
import Nestel from '../../dist/nestel';

describe("Nestel", () => {
  it('should return nested object query', () => {

  });

  it('should return flat object', () => {

  });

  it('should return an object with flat and nested values', () => {

  });
});
